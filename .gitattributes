# Normalize all text file line endings on checkout and commit
* text=auto

# Optional: If you want to ensure specific files ALWAYS have LF (Linux/macOS) endings
# (Useful for shell scripts, some config files, if you collaborate cross-platform heavily)
# *.sh eol=lf
# *.md eol=lf

# Optional: If you want to ensure specific files ALWAYS have CRLF (Windows) endings
# *.bat eol=crlf
