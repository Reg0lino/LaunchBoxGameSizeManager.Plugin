<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{D4AEB1D3-74D4-4C48-B01C-56BD42A0D34C}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>LaunchBoxGameSizeManager</RootNamespace>
    <!-- Changed RootNamespace for consistency with your using statements -->
    <AssemblyName>LaunchBoxGameSizeManager.Plugin</AssemblyName>
    <TargetFrameworkVersion>v4.7.2</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <Deterministic>true</Deterministic>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Core" />
    <Reference Include="System.Drawing" />
    <!-- Ensure this is present for .NET Framework drawing -->
    <!-- REMOVED direct reference to System.Drawing.Common -->
    <!-- <Reference Include="System.Drawing.Common, Version=8.0.0.0, Culture=neutral, PublicKeyToken=cc7b13ffcd2ddd51, processorArchitecture=MSIL" /> -->
    <Reference Include="System.Windows.Forms" />
    <!-- Ensure this is present for WinForms UI -->
    <Reference Include="System.Xml.Linq" />
    <Reference Include="System.Data.DataSetExtensions" />
    <Reference Include="Microsoft.CSharp" />
    <Reference Include="System.Data" />
    <Reference Include="System.Net.Http" />
    <Reference Include="System.Xml" />
    <Reference Include="Unbroken.LaunchBox.Plugins">
      <!-- Simplified, Version is good but not strictly needed here if HintPath is correct -->
      <SpecificVersion>False</SpecificVersion>
      <!-- This is fine, allows for minor LB updates -->
      <HintPath>lib\Unbroken.LaunchBox.Plugins.dll</HintPath>
      <!-- Ensure this path is correct relative to the .csproj file -->
    </Reference>
  </ItemGroup>
  <ItemGroup>
    <Compile Include="CorePlugin\GameSizeManagerPlugin.cs" />
    <Compile Include="Models\DeleteOperationType.cs" />
    <Compile Include="Models\GameSizeInfo.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="Services\FileSystemService.cs" />
    <Compile Include="Services\GameProcessingService.cs" />
    <Compile Include="Services\LaunchBoxDataService.cs" />
    <Compile Include="UI\PluginUIManager.cs" />
    <Compile Include="Utils\Constants.cs" />
    <Compile Include="Utils\FormatHelpers.cs" />
  </ItemGroup>
  <ItemGroup>
    <!-- It's generally better not to include files from 'lib' directly as 'None' if they are dependencies.
         The <Reference> with <HintPath> handles the DLL.
         If README.md is just for developer info, this is fine. -->
    <None Include="lib\README.md" />
  </ItemGroup>
  <ItemGroup>
    <!-- DLLs that are references should generally not be 'Content'.
         The <Reference> with <HintPath> is the correct way to reference DLLs.
         This <Content> item can cause the DLL to be copied to the output again,
         which is usually harmless but redundant if already handled by the reference.
         Consider removing this if Unbroken.LaunchBox.Plugins.dll is only a build-time reference
         and LaunchBox provides it at runtime. If you need to bundle it (not typical for LB plugins),
         there are other ways. For now, leaving it might be okay but it's not standard for references.
         If Unbroken.LaunchBox.Plugins.dll is in the GAC or LB's runtime dir, HintPath makes it found at build time.
    -->
    <!-- <Content Include="lib\Unbroken.LaunchBox.Plugins.dll" /> -->
    <!-- Consider removing this line -->
    <Content Include="lib\Unbroken.LaunchBox.Plugins.dll" />
  </ItemGroup>
  <ItemGroup>
    <!-- Folders for bin/obj are usually not explicitly listed as they are managed by the build process.
         The 'docs' folder is fine if it contains documentation you want to track with the project.
         If these <Folder> includes were added by "Show All Files" -> "Include in Project",
         they are generally harmless but not strictly necessary. -->
    <!-- <Folder Include="bin\Debug\" /> -->
    <!-- <Folder Include="bin\Release\" /> -->
    <!-- If you have actual documents here -->
    <Folder Include="docs\" />
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>